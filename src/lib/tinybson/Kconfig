menuconfig RTFRAME_TINYBSON
	bool "TinyBSON library"
	default n
	depends on POSIX_API || ARCH_POSIX
	help
	  Enable the TinyBSON serialization library.
	  Requires POSIX file I/O (open/read/write/lseek/fsync).
