menu "Core"

config RTFRAME_CORE
    bool "RTFrame core runtime"
    default y
    help
      Enable the RTFrame core runtime including work queue manager
      and task self-registration system. Required by all other modules.

endmenu # Core
