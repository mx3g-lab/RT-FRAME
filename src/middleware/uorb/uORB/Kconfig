config UORB_SUBSCRIBE_HANDLE_MAX
	int "Maximum concurrent orb_subscribe handles"
	default 64
	range 8 256
	help
	  Maximum number of simultaneous fd-style subscriptions (orb_subscribe).
	  Each slot uses sizeof(Subscription) bytes on the heap.
